/**
 * Este arquivo inclui polyfills necessários para o Angular e é carregado antes da aplicação.
 * Angular 17+ usa Zone.js por padrão via main.ts.
 * Arquivo mantido para compatibilidade com angular.json.
 */
import 'zone.js';
